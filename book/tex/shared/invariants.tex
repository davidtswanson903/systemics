% tex/shared/invariants.tex
% Conventions that keep the repo diff-friendly and cross-ref stable.

% Normative vs informative markers
\newcommand{\normative}{\textbf{(normative)}}
\newcommand{\informative}{\textbf{(informative)}}

% Labeling convention reminder:
%   - Prefix labels by unit, e.g. def:ch14-claim-space, thm:ch11-confluence
%   - Never hardcode numbers; always \ref/\eqref.
